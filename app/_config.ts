import { TimerConfig } from "./_types";

const defaultTimerConfig: TimerConfig = {
  workMinutes: 1,
  breakMinutes: 1,
  numberOfRounds: 0,
  autoStartBreak: true,
  autoStartWork: true,
};

export default defaultTimerConfig